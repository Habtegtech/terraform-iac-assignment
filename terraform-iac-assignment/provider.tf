# Provider configuration for AWS
# enable interaction with different infrastructure platforms by configuring resource management settings, 
# such as specifying the cloud provider and its parameters.
provider "aws" {
  region = "us-east-1"  #  the AWS region
}
